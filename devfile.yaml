schemaVersion: 2.0.0
metadata:
  name: aws-universal
  version: 1.0.1
  displayName: AWS Universal
  description: Stack with AWS Universal Tooling
  tags: ["aws", "al2"]
  projectType: "aws"
components:
  - name: aws-runtime
    container:
      image: IMAGE_URL
      mountSources: true
      command: ['sleep', 'infinity']
commands:
  - id: testcommand
    exec:
      component: go
      commandLine: "go"
      workingDir: "/go"
