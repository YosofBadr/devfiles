schemaVersion: 2.1.0
metadata:
  name: mydevfile
components:
  - name: python
    container:
      image: python:latest
      mountSources: true
      command: ["sleep", "infinity"]
commands:
  - id: testCommands
    exec:
      component: pythonHelp
      commandLine: "python --help"
