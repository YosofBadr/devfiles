schemaVersion: 2.0.0
metadata:
  name: public-repository
components:
  - name: go
    container:
      image: golang
      mountSources: true
      command: ['sleep', '86400']
commands:
  - id: testcommand
    exec:
      component: go
      commandLine: "go"
      workingDir: "/go"
